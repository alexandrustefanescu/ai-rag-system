# Project Rules

## Code Style

- **Maximum line length: 88 characters** for all Python files.
  The project uses `ruff` with the default 88-char limit.
  Never write Python lines longer than 88 characters.
- Run `make format` then `make lint` before considering any Python change complete.
